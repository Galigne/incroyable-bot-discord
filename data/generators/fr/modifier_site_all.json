{
  "schemaVersion": 4,
  "id": "modifier_site_all",
  "visibility": "internal",
  "name": "Modificateurs généraux de site",
  "description": "Variations durables et conditions météorologiques passagères adaptées à de nombreux lieux",
  "entrySchema": {
    "required": ["description"]
  },
  "entries": [
    {
      "id": "abandoned",
      "name": "Abandonné",
      "fields": {
        "description": "Le lieu fonctionne encore partiellement, mais ses occupants sont partis sans emporter leurs affaires essentielles."
      }
    },
    {
      "id": "immense",
      "name": "Immense",
      "fields": {
        "description": "Les distances intérieures dépassent les attentes et imposent ravitaillement, navigation ou plusieurs étapes d’exploration."
      }
    },
    {
      "id": "heavy_rain",
      "name": "{{ weather:heavy_rain.name }}",
      "weight": 2,
      "fields": {
        "description": "{{ weather:heavy_rain.description }}"
      }
    },
    {
      "id": "thunderstorm",
      "name": "{{ weather:thunderstorm.name }}",
      "fields": {
        "description": "{{ weather:thunderstorm.description }}"
      }
    },
    {
      "id": "dense_fog",
      "name": "{{ weather:dense_fog.name }}",
      "weight": 2,
      "fields": {
        "description": "{{ weather:dense_fog.description }}"
      }
    },
    {
      "id": "strong_winds",
      "name": "{{ weather:strong_winds.name }}",
      "weight": 2,
      "fields": {
        "description": "{{ weather:strong_winds.description }}"
      }
    },
    {
      "id": "heavy_snow",
      "name": "{{ weather:heavy_snow.name }}",
      "fields": {
        "description": "{{ weather:heavy_snow.description }}"
      }
    },
    {
      "id": "cold_snap",
      "name": "{{ weather:cold_snap.name }}",
      "fields": {
        "description": "{{ weather:cold_snap.description }}"
      }
    },
    {
      "id": "heat_wave",
      "name": "{{ weather:heat_wave.name }}",
      "fields": {
        "description": "{{ weather:heat_wave.description }}"
      }
    },
    {
      "id": "hailstorm",
      "name": "{{ weather:hailstorm.name }}",
      "fields": {
        "description": "{{ weather:hailstorm.description }}"
      }
    },
    {
      "id": "dust_storm",
      "name": "{{ weather:dust_storm.name }}",
      "fields": {
        "description": "{{ weather:dust_storm.description }}"
      }
    },
    {
      "id": "ashfall",
      "name": "{{ weather:ashfall.name }}",
      "fields": {
        "description": "{{ weather:ashfall.description }}"
      }
    }
  ]
}
